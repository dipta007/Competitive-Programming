#include <stdio.h>

int main()
{
    int t,n,a[15];
    while(scanf("%d %d",&t,&n)!=EOF)
    {
        for(i=0;i<n;i++)
            `scanf("%d",&a[i])

        for(j=0;j<n;j++)
        {
            for(k=0;k<n;k++)
            {
                if(a[j]+a[k]==t)

            }
        }
    }
}
